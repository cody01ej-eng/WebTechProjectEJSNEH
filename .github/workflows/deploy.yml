name: Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: production-deploy
  cancel-in-progress: true

jobs:
  backend-package:
    name: Package Backend Artifact
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Verify backend before deployment
        run: mvn -B verify

      - name: Upload backend jar
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: backend/target/project-pulse-backend-0.0.1-SNAPSHOT.jar
          if-no-files-found: error

  deploy-backend:
    name: Deploy Backend To Azure App Service
    runs-on: ubuntu-latest
    needs: backend-package
    environment: production
    env:
      AZURE_BACKEND_WEBAPP_NAME: ${{ vars.AZURE_BACKEND_WEBAPP_NAME }}
      AZURE_BACKEND_WEBAPP_PUBLISH_PROFILE: ${{ secrets.AZURE_BACKEND_WEBAPP_PUBLISH_PROFILE }}
    steps:
      - name: Download backend jar
        if: ${{ env.AZURE_BACKEND_WEBAPP_NAME != '' && env.AZURE_BACKEND_WEBAPP_PUBLISH_PROFILE != '' }}
        uses: actions/download-artifact@v4
        with:
          name: backend-jar
          path: backend-artifact

      - name: Skip backend deployment when Azure backend settings are missing
        if: ${{ env.AZURE_BACKEND_WEBAPP_NAME == '' || env.AZURE_BACKEND_WEBAPP_PUBLISH_PROFILE == '' }}
        run: echo "Skipping backend deployment because the required GitHub variable or secret is missing."

      - name: Deploy backend jar
        if: ${{ env.AZURE_BACKEND_WEBAPP_NAME != '' && env.AZURE_BACKEND_WEBAPP_PUBLISH_PROFILE != '' }}
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_BACKEND_WEBAPP_NAME }}
          publish-profile: ${{ env.AZURE_BACKEND_WEBAPP_PUBLISH_PROFILE }}
          package: backend-artifact/project-pulse-backend-0.0.1-SNAPSHOT.jar

  deploy-frontend:
    name: Deploy Frontend To Azure Static Web Apps
    runs-on: ubuntu-latest
    environment: production
    env:
      VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
      AZURE_STATIC_WEB_APPS_API_TOKEN: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
    steps:
      - name: Checkout repository
        if: ${{ env.AZURE_STATIC_WEB_APPS_API_TOKEN != '' }}
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: ${{ env.AZURE_STATIC_WEB_APPS_API_TOKEN != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        if: ${{ env.AZURE_STATIC_WEB_APPS_API_TOKEN != '' }}
        run: npm ci
        working-directory: frontend

      - name: Build frontend bundle
        if: ${{ env.AZURE_STATIC_WEB_APPS_API_TOKEN != '' }}
        run: npm run build
        working-directory: frontend

      - name: Skip frontend deployment when the Static Web Apps token is missing
        if: ${{ env.AZURE_STATIC_WEB_APPS_API_TOKEN == '' }}
        run: echo "Skipping frontend deployment because AZURE_STATIC_WEB_APPS_API_TOKEN is not configured."

      - name: Deploy frontend bundle
        if: ${{ env.AZURE_STATIC_WEB_APPS_API_TOKEN != '' }}
        uses: Azure/static-web-apps-deploy@v1
        with:
          action: upload
          app_location: frontend/dist
          output_location: ''
          skip_app_build: true
          azure_static_web_apps_api_token: ${{ env.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
